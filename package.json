{
  "name": "user_random_name_generator",
  "version": "1.0.7",
  "description": "A utility package for generating random user names",
  "main": "dist/index.js",
  "scripts": {
    "start": "concurrently \"npx tsc --watch\" \"nodemon dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["random", "username", "name-generator", "utility"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.5"
  }
}
